name: 'Terraform PR Validation'

on:
  pull_request:
    types: [opened, synchronize, reopened]  # ← SOLO en PRs activos, NO en closed
    branches: [ main ]  
    paths:
    - 'terraform/**'

permissions:
  id-token: write
  contents: read

jobs:
  terraform-validation:
    runs-on: ubuntu-latest
    environment: production  # ← Esto vincula con tu federated credential
    defaults:
      run:
        working-directory: ./terraform

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v2

    - name: Azure Login
      uses: azure/login@v1    #
      with:
        client-id: ${{ secrets.AZURE_CLIENT_ID }}
        tenant-id: ${{ secrets.AZURE_TENANT_ID }}
        subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
        client-secret: ${{ secrets.AZURE_CLIENT_SECRET }} ← Solo esto, SIN secrets

    - name: Terraform Init
      run: terraform init

    - name: Terraform Validate
      run: terraform validate

    - name: Terraform Plan
      id: plan
      run: |
        terraform plan -no-color -input=false
        echo "::set-output name=plan_exitcode::$?"
      continue-on-error: true

    #- name: Update PR with Plan Result
    #  if: github.event.pull_request.state == 'open'  # ← SOLO si PR está ABIERTO
    #  uses: actions/github-script@v6
    #  with:
    #    script: |
    #      const output = `#### Terraform Plan Result 📋
    #      \`\`\`
    #      ${process.env.PLAN_STDOUT}
    #      \`\`\`
    #      *Pushered by: @${{ github.actor }}*`;
    #      
    #      github.rest.issues.createComment({
    #        issue_number: context.issue.number,
    #        owner: context.repo.owner,
    #        repo: context.repo.repo,
    #        body: output
    #      });
    #  env:
    #    PLAN_STDOUT: ${{ steps.plan.outputs.stdout }}